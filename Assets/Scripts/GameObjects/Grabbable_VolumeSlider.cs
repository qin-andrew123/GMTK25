using UnityEngine;

public class Grabbable_VolumeSlider : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    // If player is holding, player can use right-click to create a sound blast
    // (de-couple right-click from grab)

    
}
